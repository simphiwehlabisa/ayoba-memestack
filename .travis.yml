language: node_js
node_js:
  - "stable"
cache:
  yarn: true
  directories:
    - node_modules
script:
  - yarn run build
deploy:
  provider: surge
  skip_cleanup: true
  domain: memestack.surge.sh
  project: ./build/
on:
  branch: master
