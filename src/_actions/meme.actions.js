export const memeActions = {
  search,
  trending,
};

function trending() {}

function search(query) {
  return (dispatch) => {
    let endPoint = "";
    let payload = {
      query: query,
    };
  };
}
